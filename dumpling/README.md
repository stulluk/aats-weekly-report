# dumpling (headless server)

Placeholder for **cron**-driven automation on shared build hosts (no FortiClient, no `systemd --user` without linger).

The same `scripts/` HTTP helpers will be reused; scheduling and ntfy defaults will mirror `aml-tpm-project-weekly-status-update` dumpling patterns once submit automation is defined.
